# n8n Credentials Example

Sprint 1 does not require live credentials.

When production integration begins, credentials must be configured in n8n credential storage or platform secret manager only. Do not commit tokens, API keys, webhook secrets, LINE tokens, Meta app secrets, or Supabase service keys to this repository.
